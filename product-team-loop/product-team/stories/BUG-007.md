# BUG-007 — Fix speed control not rescheduling typePrompt interval

**Meta:** id `BUG-007` · type `bug` · surface `live-demo` · priority score `1.5` · scope `UI-only` · files `product-team-loop/index.html`

## User value
As a visitor on scene 1 (Kickoff) who changes speed, I want the typing animation to run at the new speed, so the speed control actually affects the prompt typeout.

## Acceptance criteria
- [ ] Changing speed while on scene 0 restarts `typePrompt()` with the new speed
- [ ] No regression to speed control on other scenes

## Files it owns
- `product-team-loop/index.html` — speed control click handler

## Implementation
Add `if(di===0){ typePrompt(); }` after the `scheduleAuto()` call in the speed button click handler.

## DoR
- [x] value + criteria · [x] disjoint files · [x] constraints
