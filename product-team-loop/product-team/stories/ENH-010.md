# ENH-010 — Strengthen Replenish mention in hero copy

**Meta:** id `ENH-010` · type `enhancement` · surface `hero` · priority score `3.0` · scope `UI-only` · files `product-team-loop/index.html`

## User value
As a first-time visitor reading the hero, I want to immediately understand that the loop is self-replenishing — that it refills its own backlog automatically — so that I grasp the key differentiator before scrolling.

## Acceptance criteria
- [ ] The word "Replenish" or "self-replenishing" appears explicitly in the hero section (lead paragraph or pills)
- [ ] The current 4 pills are preserved; the Replenish concept is added as a 5th pill OR the "Loops until the work is done" pill is rewritten to be more explicit about the self-replenishing mechanic
- [ ] Hero lead paragraph remains ≤3 sentences; no em dashes introduced
- [ ] No AI-tell phrases (streamlined, seamless, leverage, robust)

## Files it owns
- `product-team-loop/index.html` — edit hero pills block at lines 406-411

## Contract symbols verified
| symbol | kind | grep command | result |
|--------|------|--------------|--------|
| `class="pills"` | HTML class | `grep -n "class=\"pills\""` | found at index.html:406 |
| `class="pill"` | HTML class | `grep -n "class=\"pill\""` | found at index.html:407 |

## Reuse pointers
- Existing pill pattern at lines 407-410: `<span class="pill"><b>Label</b> description text</span>`
- Current 4th pill: `<span class="pill"><b>Loops</b> until the work is done</span>` — candidate for replacement

## Implementation sketch
Replace the 4th pill with a more explicit Replenish pill:
- Old: `<span class="pill"><b>Loops</b> until the work is done</span>`
- New: `<span class="pill"><b>Self-replenishing</b> backlog, automatically refilled</span>`

## Constraints
- No em dashes; no AI-tell phrases
- Keep the existing 4-pill structure; do not add a 5th pill (layout already tuned for 4)
- `<b>` tag on the key word as per existing pill pattern

## Scope tag
`UI-only`

## Definition of Ready checklist
- [x] user value + testable acceptance criteria
- [x] disjoint files listed
- [x] all contract symbols verified-found
- [x] reuse pointers + constraints + scope tag present
