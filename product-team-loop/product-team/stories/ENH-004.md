# ENH-004 — Increase footer copyright text contrast

**Meta:** id `ENH-004` · type `enhancement` · surface `footer` · priority score `3.0` · scope `UI-only` · files `product-team-loop/index.html`

## User value
As a low-vision visitor, I want the footer copyright line readable against the dark background, so it meets WCAG AA contrast.

## Acceptance criteria
- [ ] `.footer-bottom` text color is no longer 28% opacity; it uses `var(--on-dark-muted)` (52%) or higher
- [ ] No layout change; only the color value changes

## Files it owns
- `product-team-loop/index.html` — `.footer-bottom` rule at line 360

## Contract symbols verified
| symbol | grep | result |
|--------|------|--------|
| `.footer-bottom` | `grep -n ".footer-bottom"` | index.html:360 |
| `--on-dark-muted` | site.css | defined: rgba(240,240,232,0.52) |

## Implementation
Change `color:rgba(240,240,232,.28)` → `color:var(--on-dark-muted)` in the `.footer-bottom` rule.

## Constraints
CSS var, no hardcoded color. Scope: `UI-only`.

## DoR
- [x] value + criteria · [x] disjoint files · [x] symbols verified · [x] constraints
