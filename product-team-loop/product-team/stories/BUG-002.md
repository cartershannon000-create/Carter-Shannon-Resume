# BUG-002+003 — Fix demo player boundary states

**Meta:** id `BUG-002` · type `bug` · surface `live-demo` · priority score `4.5` · scope `UI-only` · files `product-team-loop/index.html`

## User value
As a visitor watching the demo, I want the Play and Next/Back buttons to behave predictably at the start and end of the demo, so that I can control playback without getting disoriented.

## Acceptance criteria
- [ ] When the demo auto-completes at scene 8 and stops, pressing Play does NOT silently jump to scene 1 — instead Play is disabled or does nothing; the Restart button is the way to replay from scene 1
- [ ] The Next button is visually disabled (disabled attr + opacity) when on the last scene (di === N-1)
- [ ] The Back button is visually disabled when on the first scene (di === 0)
- [ ] No regression: Restart button still resets to scene 1 and plays; Play/Pause mid-demo still works; clicking Next/Back in the middle of the demo still advances correctly

## Files it owns
- `product-team-loop/index.html` — change `function play()` at line 1108; change `function dNext()`/`function dBack()` at lines 1110-1111; add disabled styling to `.dc-btn:disabled` in `<style>`; call `updateControls()` after `dShow()`

## Contract symbols verified
| symbol | kind | grep command | result |
|--------|------|--------------|--------|
| `function play` | JS function | `grep -n "function play"` | found at index.html:1108 |
| `function dNext` | JS function | `grep -n "function dNext"` | found at index.html:1110 |
| `function dBack` | JS function | `grep -n "function dBack"` | found at index.html:1111 |
| `function dShow` | JS function | `grep -n "function dShow"` | found at index.html:1090 |
| `#dNext` | button id | `grep -n "id=\"dNext\""` | found at index.html:898 |
| `#dBack` | button id | `grep -n "id=\"dBack\""` | found at index.html:893 |
| `#dPlay` | button id | `grep -n "id=\"dPlay\""` | found at index.html:894 |
| `.dc-btn` | CSS class | `grep -n "\.dc-btn"` | found at index.html:334 |

## Reuse pointers
- `.dc-btn` class at index.html:334 for button styling reference
- `updatePlay()` pattern at index.html:1107 — add analogous `updateControls()` for disable state

## Implementation sketch
1. In the `<style>` block, add after `.dc-btn:hover`:
   `.dc-btn:disabled { opacity: 0.35; cursor: not-allowed; pointer-events: none; }`
2. Add a helper after `updatePlay()`:
   `function updateControls(){ document.getElementById('dBack').disabled = di<=0; document.getElementById('dNext').disabled = di>=N-1; }`
3. Call `updateControls()` at the end of `dShow(i)` (line 1098) and at the end of `pause()`
4. Fix `play()` to not reset: `function play(){ if(di>=N-1) return; playing=true; updatePlay(); scheduleAuto(); }`

## Constraints
- No new HTML structure, CSS variables only (no hardcoded colors)
- The Restart button must still work correctly (calls `dShow(0); play()`)

## Scope tag
`UI-only`

## Definition of Ready checklist
- [x] user value + testable acceptance criteria
- [x] disjoint files listed
- [x] all contract symbols verified-found
- [x] reuse pointers + constraints + scope tag present
