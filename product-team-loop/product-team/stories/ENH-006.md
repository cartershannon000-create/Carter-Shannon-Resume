# ENH-006 — Forward CTA when the demo ends

**Meta:** id `ENH-006` · type `enhancement` · surface `live-demo` · priority score `2.3` · scope `UI-only` · files `product-team-loop/index.html`

## User value
As a visitor who just watched the full demo, I want a clear next step, so I move into the deeper material instead of hitting a dead end.

## Acceptance criteria
- [ ] The final scene (scene 8, replenish, `data-scene="7"`) ends with a CTA button that returns to the Overview pane
- [ ] The CTA reuses the existing `data-goto="overview"` mechanism (no new JS handler needed)
- [ ] Button uses existing `.btn` or `.dc-btn` styling; no hardcoded colors
- [ ] No em dashes or AI-tell phrases in the CTA copy
- [ ] No regression to the replenish scene animation

## Files it owns
- `product-team-loop/index.html` — inside the `data-scene="7"` replenish scene block (around lines 875-896), after the closing `src-note`

## Contract symbols verified
| symbol | grep | result |
|--------|------|--------|
| `data-scene="7"` | `grep -n` | index.html (replenish scene) |
| `data-goto` handler | `grep -n "data-goto"` | index.html:931 (preventDefault + show) |
| `.btn` | site page style | index.html:158 area |

## Implementation
At the end of the replenish scene's `.replwrap` (after the final `<p class="src-note">`), add:
```html
<button class="btn" data-goto="overview" style="margin-top:20px">See why the output isn't slop</button>
```
The existing `[data-goto]` listener already switches panes and scrolls to top.

## Constraints
Reuse existing CTA mechanism. No new handler. Scope: `UI-only`.

## DoR
- [x] value + criteria · [x] disjoint files · [x] symbols verified · [x] constraints
