# ENH-003 — Increase retchip hit target to 44px minimum

**Meta:** id `ENH-003` · type `enhancement` · surface `loopband` · priority score `3.0` · scope `UI-only` · files `product-team-loop/index.html`

## User value
As a mobile visitor, I want the Replenish chip button on the orbit ring to have a large enough tap area, so that I can reliably tap it without missing on a small screen.

## Acceptance criteria
- [ ] `.retchip` has a minimum height of 44px (WCAG 2.5.5 Level AAA target)
- [ ] Visual appearance stays consistent with the existing dashed chip style (green-soft background, dashed border, "Replenish" label with icon)
- [ ] Chip still sits correctly over the orbit ring at its current position (left:20.5%; top:20.5%)

## Files it owns
- `product-team-loop/index.html` — update `.retchip` CSS rule at line 125 to increase padding for 44px height

## Contract symbols verified
| symbol | kind | grep command | result |
|--------|------|--------------|--------|
| `.retchip` | CSS class | `grep -n "\.retchip"` | found at index.html:125 |
| `class="retchip callout"` | HTML class | `grep -n "retchip callout"` | found at index.html:482 |

## Reuse pointers
- `.retchip` rule at line 125: `padding:6px 11px` — increase vertical padding to `padding:12px 14px` to reach ≥44px rendered height with 9.5px font + line-height:1

## Constraints
- No change to font-size, border style, background, or z-index
- CSS variables only — no hardcoded colors

## Scope tag
`UI-only`

## Definition of Ready checklist
- [x] user value + testable acceptance criteria
- [x] disjoint files listed
- [x] all contract symbols verified-found
- [x] reuse pointers + constraints + scope tag present
